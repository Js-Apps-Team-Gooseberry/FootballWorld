<div class="row comment" id="{{_id}}">
    <div class="col-md-2">
        <img src="{{author.profilePicture}}" alt="{{author.username}}" class="img-responsive img-circle">
    </div>
    <div class="col-md-10">
        <p class="inline-block"><span class="bold">{{author.username}}</span> <span class="italic small">{{#date createdOn}}{{/date}}</span></p>
        <p class="btn-news-delete-comment inline-block pull-right small btn-comment-delete" onclick="return confirm('Are you sure you want to delete this comment?')">Delete</p>
        <p class="comment-content">{{content}}</p>
    </div>
</div>